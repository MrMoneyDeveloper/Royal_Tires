APP_USERNAME=
APP_PASSWORD=
DATABASE_URL=sqlite:///./asset_requests.db
FRONTEND_URL=http://localhost:5173,http://127.0.0.1:5173

# Zendesk credentials are configured server-side only.
ZENDESK_SUBDOMAIN=
ZENDESK_EMAIL=
ZENDESK_API_TOKEN=

# Zendesk workflow configuration. Keep the secret server-side.
ZENDESK_WEBHOOK_SECRET=
ZENDESK_NOTIFICATION_EMAIL=farhaanhotd1@gmail.com

# Render supplies this automatically in production. Set an HTTPS URL locally
# only when testing webhook provisioning against a reachable development host.
RENDER_EXTERNAL_URL=
